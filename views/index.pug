doctype html
html
  head
    style
      include style.css

  body
    h1= title
    p Welcome to #{title}

    each message in messages
      div(class='message')
        p= message.text
        p= message.user
        p= message.added
